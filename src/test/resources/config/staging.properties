# ============================================================
# STAGING Environment Configuration
# ============================================================
# Overrides values from config.properties for the STAGING environment.
# Only specify properties that differ from the defaults.
# ============================================================

base.url=https://opensource-demo.orangehrmlive.com

# Staging might use different timeouts or browser settings
# timeout=60000
# headless=false
